use::reg::item::RegistryItem
use::reg::item::ItemRarity

dispatch minecraft:storage["reg:shop_pool"] to struct{
    reg?: [ShopPool],
    this?: [RegistryItem]
}

struct ShopPool{
    [ItemRarity]?: [RegistryItem],
}